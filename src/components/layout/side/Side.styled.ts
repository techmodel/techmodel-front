import styled from 'styled-components';

const Side = styled.aside`
    height: 100%;
    width: var(--side-viewport-width);
    position: relative;
    background-color: #dee2e3;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.3),
    inset 0 0 10px 5px rgba(0, 0, 0, 0.3);
`;

export default Side;
